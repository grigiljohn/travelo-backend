package com.travelo.adservice.entity.enums;

public enum AdType {
    IMAGE,
    /** Static or HTML-style display unit (DB / legacy payloads may use `DISPLAY`). */
    DISPLAY,
    VIDEO,
    CAROUSEL,
    RESPONSIVE,
    TEXT,
    APP_INSTALL
}

