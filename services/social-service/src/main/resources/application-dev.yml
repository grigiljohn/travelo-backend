# Profile: dev (default) — three Postgres DBs, Redis, Kafka; optional -Dspring.profiles.active=dev,local for tiny pools
management:
  metrics:
    tags:
      service: social-service
      environment: dev
