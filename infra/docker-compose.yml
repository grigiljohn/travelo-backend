# -------------------------------------------------------------------------
# Deprecated. The canonical compose file for local development now lives at
# the repo root: ../docker-compose.yml
#
# Run from the repo root:
#     docker compose up -d --build
# or:
#     ./scripts/local-up.ps1 -Build   # Windows helper
#
# This file is kept as a stub so legacy tooling doesn't break; it no longer
# defines services (the root file supersedes it).
# -------------------------------------------------------------------------
services: {}
