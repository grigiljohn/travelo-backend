[build]
builder = "DOCKERFILE"
dockerfilePath = "services/identity-service/Dockerfile"
watchPatterns = ["services/identity-service/**", "libs/**", "pom.xml"]

[deploy]
healthcheckPath = "/actuator/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
